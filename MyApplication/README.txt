How to install and use:
Android 6.0, Tested with emulator and app-permissions to modificate the calendar.

Platform has to have one calendar account configured.

The application syncs the calendar and the cloud after every change.

The basic usage with buttons. The edit happens via pressing the item on the list.

